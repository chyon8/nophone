# HANDOFF.md — 세션 인계 문서

> 새 세션(또는 다른 사람)이 여기서부터 이어가기 위한 문서. 최신 논의: **2026-07-24** (키워드 제안 전환 + 정식 UI + 단일마이크 롤플레이 테스트 하네스 — 아래 §2026-07-24, 커밋 대기 중).
> 더 자세한 배경은 [DESIGN.md](DESIGN.md) · [PLAN.md](PLAN.md) · [TESTING.md](TESTING.md) · [SCORING_SPEC.md](SCORING_SPEC.md) 참고. 이 문서는 그것들의 "지금 어디까지 왔고 다음이 뭔지" 요약이다.

## 프로젝트가 뭔가

위시켓 매니저가 프로젝트 검수·공고 등록을 위해 의뢰인과 통화할 때, **고객 말이 끝나면 AI가 "다음에 그대로 읽을 질문"을 화면에 띄워** 인지 부담을 없애는 통화 코파일럿. 기록·요약은 부산물이지 목적이 아니다. (DESIGN.md "진짜 문제", 2026-07-13 개정)

## 이번 세션에서 있었던 일 (시간순)

1. **Cluely(범용 화상회의 AI 코파일럿)와 비교 검토** — 구조는 비슷하지만(오디오→전사→LLM 실시간 지원, 기록=부산물), 결정적으로 다른 건 ① 온디맨드 Q&A가 아니라 **프로액티브** 질문 제안 ② **위시켓 도메인 특화**(범용 도구가 못 하는 부분) ③ 은폐 기능 불필요(전화라 화면 공유 자체가 없음). 방향은 맞다고 결론.
2. **DESIGN.md "진짜 문제" 개정** — 옛 정의(통화 지식 팀 자산화·기록 중심)를 "질문 제안·인지부담 최소화, 기록=부산물"로 교체. 성공 기준을 **품질 1순위, 속도는 제약(2~3초 바닥선)**으로 확정 — 처음엔 "속도가 핵심 지표"였는데, 느려도 좋은 질문이 빨라도 쓸모없는 질문보다 낫다는 논리로 뒤집음.
3. **`suggest.py` 프로토타입 (v1)** — 화면 없는 터미널 제안 루프. 모델 A/B 테스트: `gpt-4.1`이 속도(첫토큰 ~570ms)·품질(자연스러운 구어체) 둘 다 1위, `gpt-5-mini`는 느리고 괄호 나열식 만연체라 "그대로 읽기" 원칙 위반으로 탈락. PLAN.md V4로 기록.
4. **실제 샘플 통화 전사 → replay 모드로 품질 확인** — 결함 3개 발견: ① 방금 들은 답을 무시하고 직전 질문 반복 ② 이미 물은 걸 또 물음 ③ 한 번에 질문 두 개.
5. **사용자가 `SCORING_SPEC.md` 제시** — 이전에 만들어둔 "12섹션 confidence 채점 → 완성도 계산 → 질문 우선순위 → 공고 draft 출력" 스펙. 이걸 정식 채점 기준으로 채택하기로 결정.
6. **설계: 2루프 구조** — 매 발화마다 12섹션 전체를 분석하면 느려지므로 분리:
   - **빠른 루프**(고객 발화 종료마다, ~0.7초): 섹션 상태 + 최근 대화 원문 → 대사 1개 스트리밍
   - **느린 루프**(백그라운드): 대화록 전체 재채점 → 12섹션 confidence 갱신
   - **통화 전**: 사전 브리핑 1회 채점 (뭐가 비었는지 알고 시작)
   - **통화 후**: 종료 조건(완성도≥70 + 핵심 3섹션≥50) 충족 시 **공고 draft 생성 — 아직 미구현**
   - DESIGN.md에 "제안 엔진" 섹션으로 반영.
7. **구현 (v2)** — `scoring.py` 신규(채점 계층: confidence는 LLM, 합산·우선도·종료 판별은 코드), `suggest.py` 전면 개정(체크리스트를 SCORING_SPEC 12섹션으로 교체, 절차 안내 모드 추가, 직전 제안 반복 방지).
8. **replay로 재검증** — 결함 3개 해소 확인, 속도 평균 725ms 유지, 채점표가 실제 통화 내용과 부합.
9. **커밋·푸시 완료** (`273b998`, `7f3d7f1`).

## 지금 상태

- **완료**: DB(A), 서버+통화세션(B, M1), STT 실통화 검증(V3), 제안 LLM 결정(V4=gpt-4.1), 제안 엔진 v2(D0), **키워드 전환 + 정식 UI + 서버 통합(청크 C3, 2026-07-24)** — 헤드리스 검증까지.
- **미검증(사용자 몫)**: **실제 마이크 구간** — 브라우저에서 말하며 실시간 전사·키워드 제안 체감(주말 집 테스트). 실제 사람 2인 음성(협대역 포함)은 여전히 미검증.
- **아직 안 만든 것**: 공고 draft 생성(SCORING_SPEC 최종 출력물, 종료조건 충족 시), 중간 도구(검색·견적기 — 다음 페이즈).

## 2026-07-21 세션 — 집에서 전화기 없이 검증할 "롤플레이 시뮬 모드" 결정

**맥락**: 다음 우선순위는 "제안 엔진 v2를 실제 통화(반응형 상호작용)로 검증"인데, 집에는 전화기(USB 2채널 하드웨어)가 없다. replay는 고정 대본이라 **고객이 AI 제안에 반응하지 않아** 상호작용 품질의 진짜 시험대가 못 된다는 게 이미 짚은 한계. 그래서 하드웨어 없이 반응형 상대를 만드는 방법을 논의했다.

**검토한 선택지** (고객 역을 누가 하느냐):
- **A. 내가 고객(음성), 시스템이 코파일럿** — 빌드 작음. 단 내가 고객을 연기해 실제 업무 자세와 다름.
- **B. 내가 매니저, AI가 고객** ← **채택**. 코파일럿 제안을 내가 매니저로서 내보내면 AI 고객 시뮬레이터가 페르소나대로 반응. 실제 업무 흐름과 가장 같고, 미검증인 "상호작용 품질"을 정면으로 시험.
- **C. 완전 자동 (AI가 양쪽)** — B가 되면 거의 공짜로 딸려옴(매니저 턴을 직전 제안으로 자동 주입). 무시·재질문·이중질문 회귀 테스트용. B 검증 후 붙임.

**추가 결정 (2026-07-21)**: **음성 안 씀. 매니저 턴은 타이핑.** → 마이크·STT 아예 불필요. 순수 텍스트 대화 루프(= replay의 진화판인데 상대가 반응형 AI 고객).

### B(타이핑) — ✅ 구현·검증 완료 (2026-07-21)

**바로 돌리는 법** — 두 방식 (둘 다 AI 고객은 텍스트로 답함):
```bash
# 시뮬1 — 매니저 대사를 '타이핑'
uv run suggest.py --simulate sample/scenario_example.txt --briefing-file sample/briefing_coreline.txt
# 시뮬2 — 매니저 대사를 '마이크로 말하기'(STT). 위에 --voice만 추가
uv run suggest.py --simulate sample/scenario_example.txt --briefing-file sample/briefing_coreline.txt --voice
```
- 시뮬1(타이핑): `나(매니저) >` 에 대사 입력 → AI 고객 반응 → `💬` 제안 + `📊` 채점. 빈 줄/`/quit` 종료.
- 시뮬2(음성): 맥북 마이크(`find_device("MacBook")`, 장치 자동 탐색)에 말하면 STT가 받아 `[나]` 턴 → 그 뒤는 동일. Ctrl+C 종료. (실전 '말하기'를 예행연습)
- **브리핑은 권장(옵션 아님)** — 없이 시작하면 코파일럿이 프로젝트를 콜드스타트에서 환각한다(실측: 브리핑 없을 때 "전원 연계 방송 플랫폼"으로 지어냄 → 붙이니 사라짐).

**만든 것**: `customer_sim.py`(AI 고객 respond), `suggest.py`에 `--simulate`(타이핑=`simulate()`)와 `--voice`(음성=`simulate_voice()`, 마이크→STT→'me' 턴마다 AI 고객 주입) 분기, `sample/scenario_example.txt`(합성 시나리오, 새로 만들 땐 복사해 수정), `sample/briefing_coreline.txt`(시나리오와 짝인 등록 정보). sample/은 gitignore라 이 2개는 커밋 안 됨 — 새 환경이면 다시 만들어야 함(내용은 이 문서에 요지 있음, 시나리오=필라테스 3지점 회원권·예약 앱).

**스모크 검증 결과**: 반응형 대화 정상, 답변무시·재질문·이중질문 미발생, 채점 43→54% 순증, 제안 첫글자 평균 ~650ms. 남은 튜닝은 이 모드로 계속.

**참고 — 원래 스펙** (구현 근거):
새 모드 `suggest.py --simulate <시나리오파일> [--briefing-file <브리핑>]`:
1. 시나리오 파일(= AI 고객의 "진실+페르소나")과 브리핑(= 매니저가 통화 전 아는 부분 정보) 로드. 브리핑 있으면 통화 전 1회 채점(기존 그대로).
2. **매니저 턴**: 프롬프트에서 `input()`으로 한 줄 받음 → `transcript`에 `("me", text)` 추가. (마이크·STT 없음)
3. **AI 고객 턴**: `customer_sim.respond(...)` → `("customer", text)` 주입 + 화면 표시.
4. 그 고객 턴이 **기존 `on_event("customer","completed")` 경로를 그대로 트리거** → `💬` 제안 + 백그라운드 `📊` 재채점 (지금 코드 손 안 댐).
5. 2로 반복. 빈 입력 또는 `/quit` → 종료 → `report()`.

- **`customer_sim.py` 신규(작음)**: `async def respond(client, model, scenario, transcript_lines) -> str`. 페르소나 프롬프트 = *IT 프로젝트 의뢰인, `scenario`가 진실. 매니저 말에 자연스럽게 답하되 **가끔 모호하게·되묻고, 정보를 한 번에 다 쏟지 않음**. 1~3문장 구어체. 이미 말한 것 반복 안 함. 절차·정책은 의뢰인답게(모르면 모른다고).* 모델은 gpt-4.1 기본.
- **`sample/scenario_example.txt` 신규**: 합성 시나리오 1개(실제 고객 아님) + 템플릿 주석. sample/은 gitignore라 로컬 전용.
- **안 하는 것(지금)**: 고객 목소리 TTS(고객 대사는 화면으로 읽음, Phase 2 영역), C(완전 자동)는 B 검증 후.

**검증 기준**: 매니저 오프닝 타이핑 → AI 고객이 반응형으로 답 → 코파일럿이 매니저 다음 대사 제안 → 반복하며 채점 오름. **replay에서 안 잡히던 상호작용 결함(답변 무시·재질문·이중질문)이 반응형 상대에서도 안 나오는지** 확인. 이게 청크 D 품질 검증 도구 겸 H1(리허설, 하드웨어 없는 선행분).

**진행 규칙**: 위는 "설계 확정"까지다. 새 세션에서 구현 **시작 전에** PLAN.md 다시 읽고, 사용자에게 "시작해" 명시적 지시 받은 뒤 코드 착수 (CLAUDE.md 룰 7).

## 2026-07-23 세션 — Discord + BlackHole로 실제 대화 테스트 세팅 결정 + VoIP 전환 논의

**맥락**: 시뮬 모드(AI 고객 상대 타이핑/음성)로 품질 검증은 됐지만, **진짜 사람이 고객 역할을 해주는 실제 대화 테스트**가 필요하다. 물리 전화기(USB 2채널 하드웨어) 없이도 음성 통화를 잡을 수 있는 방법을 논의했다.

### 결정: Discord 음성채널 + BlackHole (가상 오디오 드라이버)

**왜 Discord인가**: 무료, 즉시 사용 가능, 별도 계정 생성 불필요. 친구(또는 동료)에게 "고객 역할 해줘" 하고 Discord 음성채널에서 통화하면 된다.

**핵심 문제**: `capture.py`는 **두 개의 별도 오디오 장치**(고객=USB 사운드카드, 나=내장마이크)로 화자를 구분한다. Discord 단일 통화를 하나의 스트림으로 잡으면 고객/나 구분이 안 됨. → **BlackHole로 Discord 출력을 별도 가상 장치로 분리**하면 기존 코드 변경 없이 해결.

```
[고객 역할 (친구 폰/PC)] ──Discord 음성채널──┐
                                              ↓
                                     Discord 출력
                                              ↓
                                     BlackHole (가상 오디오)  ← capture.py의 customer_device
[나 (맥북)] ──────────────────────── 맥북 내장 마이크        ← capture.py의 me_device
```

### 내일(2026-07-24) 사무실 세팅 순서

1. **BlackHole 설치** (`brew install blackhole-2ch` 또는 공식 사이트에서 pkg)
2. **macOS 오디오 설정**:
   - 시스템 설정 → 사운드 → 출력은 맥북 스피커/헤드폰 유지
   - Audio MIDI Setup 앱에서 **다중 출력 장치** 생성: 맥북 스피커 + BlackHole (내가 들으면서 동시에 BlackHole로도 보냄)
   - Discord 설정 → 음성 → 출력 장치를 이 다중 출력 장치로 설정
3. **장치 확인**:
   ```bash
   uv run capture.py --list   # BlackHole이 입력 장치 목록에 보이는지 확인
   ```
4. **테스트 캡처**:
   ```bash
   uv run capture.py --customer <BlackHole번호> --me <MacBook번호> --seconds 10
   # → recordings/test_customer.wav, test_me.wav 생성 → 각각 확인
   ```
5. **실전 테스트** — `server.py`의 `CUSTOMER_DEVICE_NAME`을 `"BlackHole"`로 변경하거나, 장치 번호 직접 지정:
   ```bash
   # suggest.py로 먼저 (터미널에서 확인)
   uv run suggest.py --briefing-file <브리핑파일>
   # 이때 capture.py가 "BlackHole"과 "MacBook"을 자동 탐색하도록
   # server.py의 CUSTOMER_DEVICE_NAME = "BlackHole" 로 변경 필요
   ```
6. Discord 음성채널에서 친구/동료가 고객 역할 → 실제 대화하면서 `💬` 제안 + `📊` 채점 확인

### 기대 검증 포인트
- **실제 사람 목소리 + 자유 발화**에서 STT 정확도 (시뮬은 텍스트라 STT를 거치지 않았음)
- 사람이 예상 못한 말을 했을 때 제안 품질
- 실시간 지연 체감 (Discord 지연 + STT + LLM)
- 두 채널(BlackHole/MacBook)이 깨끗하게 분리되는지

### VoIP 전환 방향 (별도 논의 — 장기)

현재 물리 하드웨어(IP전화기 → Y분배기 → USB 사운드카드)는 과도기용이다. DESIGN.md에 이미 적힌 대로:

> *"Phase 2에서는 클라우드 텔레포니(Twilio/SIP) + Vapi/LiveKit으로 전면 이주한다"*

**VoIP(인터넷 전화)의 장점**:
- API 레벨에서 화자 분리 자동 (하드웨어 불필요)
- 가상번호 발급 → 기존 회사번호 우회 가능
- 녹음·전사를 클라우드에서 처리
- 팀 확장 시 계정만 추가 (각자 하드웨어 세팅 불필요)
- 어디서든 웹으로 사용

**지금은 안 한다** — Discord 테스트로 제안 엔진 품질을 먼저 잡고, Phase 2에서 Twilio/SIP으로 전면 이주. 번호는 우회해서 쓸 수 있으므로 인터넷 전화로의 전환이 물리적 전화기를 완전히 대체 가능.

## 2026-07-24 세션 — 키워드 제안 전환 + 정식 UI + 단일마이크 롤플레이 테스트 하네스

**맥락**: 집에서 `--voice` 시뮬을 돌려본 피드백 → 방향 조정 + 이번 주말 집에서 혼자 반복 테스트할 환경을 완성하는 게 목표였다. 상세 결정·이유는 **[DESIGN.md](DESIGN.md) "2026-07-24 개정"** 에 기록됨. 요지:

1. **제안 형식: 완성문 → 키워드 큐** (텔레프롬프터 원칙 수정). 예: `예산 3천 상한 확정?` · `2단계 커머스 범위?`. 이유: 사용자가 도메인 전문가라 완성문 읽는 것보다 핵심 큐만 받아 자기 말로 묻는 게 빠르고 자연스러움("주저리주저리"가 방해). **Phase 2 TTS 자동화 땐 완성문 모드를 프롬프트 스위치로 되살릴 수 있음.**
2. **실시간 전사 표시 + AI는 발화종료에** — STT 델타를 화면에 바로바로(이미 `partial` 방송됨), LLM 제안은 발화 종료 트리거 유지(룰 3 = 매 단어 재생성 금지, 유지).
3. **테스트 셋업 = 단일 마이크 롤플레이.** 물리 전화기·**Discord·BlackHole 불필요**(말하는 사람이 나 하나라 채널 분리기 필요 없음 — 07-23의 Discord 계획은 "실제 2인 음성"일 때만). 역할 토글: **[고객] 모드(주력)** 내 마이크=고객채널, 코파일럿이 매니저 질문 생성, 내가 지저분한 고객을 연기해 꼬리질문 품질 시험 / **[매니저] 모드** 내 마이크=나채널, AI 고객(customer_sim)이 시나리오대로 반응. 브리핑 온/오프 토글(끄면 콜드스타트).
4. **STT 엔진 현행 유지**(`gpt-4o-transcribe` — 델타 스트리밍이 실시간 표시에 필요). 로컬/저가 교체는 협대역 실측 있을 때 별도 판단.
5. **중간 도구(검색·견적기) = 다음 페이즈** — 이번 UI에서 제외.

**구현·검증 완료 (청크 1~5, 커밋 대기)**:
- `suggest.py` — SYSTEM 프롬프트 키워드화, `Suggester`를 `emit` 콜백으로 출력-비의존화(터미널=`_terminal_emit`).
- `server.py` — 제안엔진+채점 통합, `suggest_*`/`score` WS 방송, `role`·`briefing`·`scenario` 파라미터, 매니저 모드 AI 고객 주입(`_inject_customer`).
- `capture.py` — `find_device` 이사(server↔suggest 순환 import 해소).
- `static/index.html` — **정식 UI로 교체**: 사전설정 화면(역할·브리핑·시나리오) + 통화 화면(대화록·키워드 제안·12섹션 요구사항). 견적/검색 실행부는 제외.
- **검증**: 터미널 replay 키워드 ✅ · 서버 WS 전 경로(`--source`) ✅ · 매니저 주입 헤드리스 ✅ · 페이지 서빙 200 ✅. **마이크 구간은 브라우저에서 사용자 검증(주말)**.

**미결(사용자 확인 대기)** — STYLE.md 관련 2개:
- ① 통화 화면 우측이 지금 전부 크림색(견적 다크 카드를 뺐더니 크림/다크 리듬 없음). 이대로 둘지 vs "다음 페이즈" 다크 플레이스홀더로 리듬 유지할지.
- ② 실시간 상태 표시(● + teal 점) 패턴을 STYLE.md에 정식 컴포넌트로 추가할지(하단 노트가 예고해둠).

**돌리는 법**: `uv run server.py` → 브라우저 http://localhost:8000 → 역할 "내가 고객" + 브리핑 → 통화 시작 → 마이크로 지저분한 고객 연기 → 실시간 전사+키워드 제안+채점 확인.

## 다음에 할 일 (우선순위순)

1. 🔜 **주말 집 테스트 — 단일 마이크 롤플레이로 키워드 제안 품질 튜닝 (2026-07-24 완성).** `uv run server.py` → [고객] 모드로 지저분한 고객 연기 → **꼬리질문·확장질문**이 잘 나오는지가 핵심. 다양한 의뢰인 유형·콜드스타트(브리핑 오프)·통화 끝까지(종료조건 도달)까지. → 실측 피드백으로 `suggest.py` SYSTEM 프롬프트 조이기.
   - ~~Discord + BlackHole~~ 는 **실제 2인 음성**(친구가 고객 역)일 때만. 혼자 하는 반복 테스트엔 단일 마이크 하네스가 대체함(07-24 결정). 2인 음성 갈 때 `CUSTOMER_DEVICE_NAME`을 `"BlackHole"`로 바꾸는 건 그대로 유효.
2. ✅ ~~롤플레이 시뮬 모드 B(타이핑) — 구현·스모크 검증 완료 (2026-07-21).~~ **정식 UI로 승격됨 (07-24, 청크 C3).** `--simulate` 터미널 모드도 그대로 유지.
3. **(하드웨어 있을 때) 실제 통화 1건으로 `suggest.py` v2 검증** — IP전화기 + USB 사운드카드로 진짜 의뢰인 통화 테스트. Discord 테스트 결과가 좋으면 이걸 건너뛰고 VoIP로 갈 수도 있음.
   ```bash
   uv run check_devices.py            # 장치 번호 확인
   uv run check_devices.py <번호>      # 신호 확인
   uv run suggest.py --briefing-file <사전브리핑파일>   # 실전 (장치 자동 탐색)
   ```
4. **공고 draft 생성** — 종료 조건 충족 시 SCORING_SPEC §8 스키마대로 마크다운 공고문 출력. 통화 후(F1) 처리로, replay/시뮬 데이터로도 먼저 만들어볼 수 있음.
5. 위가 안정되면 **청크 C(통화 화면 UI)** — 지금까지는 전부 터미널 프로토타입이었고 화면이 없었음. UI는 제안 엔진 품질이 잡힌 뒤에 붙이기로 사용자가 확정.

## 알아둘 것 (착각하기 쉬운 부분)

- **`suggest.py`의 기본 모델은 `gpt-4.1`** (실측 승자). `gpt-5-mini`가 아님 — PLAN.md 원래 계획에서 바뀐 부분.
- **`sample/`, `data/`, `glossary.txt`는 gitignore 대상.** replay 대본(`sample/replay_call.txt`)과 브리핑 예시(`sample/briefing_example.txt`)는 로컬에만 있고 git엔 없음 — 실제 고객 통화 내용이 들어있기 때문. 새 환경에서 이어가려면 이 파일들을 다시 만들거나 실통화로 대체해야 함.
- **`suggest.py`의 `WISHKET_PROCESS` 상수(파일 상단)는 사용자 확인 대기 중** — 절차 안내(수수료 등)의 근거 텍스트인데, 지금은 전사에서 확인된 것만 넣어놨고 실제 안내 멘트 기준 보강이 필요하다고 지난 세션에 짚었음. 아직 답 없음.
- **개발 룰(CLAUDE.md 룰 7)**: 청크 완료 후 무조건 멈추고 사용자 컨펌 받은 뒤 다음 단계. "계획을 말한 것"과 "시작 허락"은 별개 — "시작해" 류 명시적 지시 전까지 코드 건드리지 않음.
- **커밋은 사용자가 직접 실행** (룰 8) — Claude는 복붙용 git 명령 블록만 제공.
